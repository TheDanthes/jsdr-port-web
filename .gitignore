# Dumps y datos de producción — NUNCA al repo
dumps/*
!dumps/.gitkeep
*.dump
*.sql.gz
*.backup

# Motor tipográfico: binarios compilados
composer/src-c/**/*.o
composer/src-c/**/*.a
composer/bin/

# Node
node_modules/
dist/
build/
*.tsbuildinfo
.vite/

# Entorno
.env
.env.*
!.env.example

# Salida de fotocomposición en desarrollo
salida-indesign/

# Editores / SO
.vscode/
.idea/
.DS_Store
Thumbs.db

# Deja afuera 8 archivos de respaldo de 2005 que quedaron en `fonts/anchura/`
# y `formatos/formatos_anteriores/` del motor. Se comprobó apartándolos y
# volviendo a correr el banco: 534/534 igual. El motor abre cada archivo por
# nombre exacto y esos no los abre nunca.
*~
